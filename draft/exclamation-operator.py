# The exclamation mark operator `!` can be used to throw an error if a value is nil:
let type = node.attributes.type!.name

# Reminder: in Fa, there is no throw. Instead, a special value is passed to the parent function.
