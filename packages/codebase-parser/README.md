# Codebase Parser

The Fa codebase parser is a tool that parses a whole codebase using very fast and efficient parallel workers.

It can be used to:

- Compile a Fa project,
- Analyze a codebase,
- Create a very fast and efficient language server.
