# It's not easy to interface C++ with Fa, because C++ code can have so many organisations
# That's why tools like makefiles and CMake were built
# C++ interfacing with Fa is interesting only with big frameworks (like for GUI, or games)
# With small files you can easily rewrite the code in Fa.
